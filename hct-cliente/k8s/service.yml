apiVersion: v1
kind: Service
metadata:
  name: hct-cliente-service
  namespace: hct-alquiler-vehiculos
spec:
  type: ClusterIP
  selector:
    app: hct-cliente
  ports:
    - port: 8082
      targetPort: 8082
